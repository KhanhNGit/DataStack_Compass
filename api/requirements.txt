# =============================================================================
# DataStack Compass — FastAPI Backend Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Run dev: uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
# =============================================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Pydantic (included by FastAPI, pinned for compatibility)
pydantic==2.7.4

# StarRocks / MySQL connector
pymysql==1.1.1
cryptography==42.0.8

# Connection pooling
DBUtils==3.1.0

# HTTP client (for health checks, external calls)
httpx==0.27.0

# Environment variables
python-dotenv==1.0.1
